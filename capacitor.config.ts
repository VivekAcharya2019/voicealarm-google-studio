import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vivekacharya.smartvoicealarm',
  appName: 'smartvoicealarm',
  webDir: 'dist'
};

export default config;
